import 'package:flutter/material.dart';

class AppColors {
  // Primary - Warm, calming
  static const Color primary = Color(0xFF8B6F47); // Warm brown
  static const Color primaryLight = Color(0xFFA58968);
  static const Color primaryDark = Color(0xFF6B5435);

  // Accent - Soft, approachable
  static const Color accent = Color(0xFFD4A574); // Latte color
  static const Color accentLight = Color(0xFFE8C9A0);

  // Backgrounds
  static const Color background = Color(0xFFFAF8F5); // Warm white
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F1EC);

  // Text
  static const Color textPrimary = Color(0xFF2C2420);
  static const Color textSecondary = Color(0xFF6B5F57);
  static const Color textTertiary = Color(0xFF9B8F87);

  // Mood colors (subtle)
  static const Color moodLow = Color(0xFFB8C5D6); // Soft blue
  static const Color moodOkay = Color(0xFFD4C9A8); // Neutral beige
  static const Color moodHigh = Color(0xFFE8B4A0); // Warm peach

  // Status
  static const Color success = Color(0xFF7BA369);
  static const Color error = Color(0xFFD07A6E);
  static const Color warning = Color(0xFFE8B86D);

  // Premium
  static const Color premium = Color(0xFFD4AF37); // Gold
}

class AppConstants {
  // App Info
  static const String appName = 'MoodBrew';
  static const String appTagline =
      'Tell us how you feel. We\'ll handle the coffee.';

  // Timing
  static const Duration splashDuration = Duration(seconds: 2);
  static const Duration animationDuration = Duration(milliseconds: 300);

  // Limits
  static const int maxSavedMoments = 100;
}
